describe('TranslatorService', () => {
  it('should connect to the page and request the translation', function () {});

  it('should re-try translation if it fails to access page', function () {});

  it('should re-try translation if it fails to translate', function () {});

  it('should clean up the all resources after translation', function () {});

  it('should cancel the translation if there is a cancel flag', function () {});
});
